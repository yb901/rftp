apiVersion: v1
kind: Service
metadata:
  name: rfpt-performance
  namespace: prod
  labels:
    app.kubernetes.io/name: rfpt-performance
    app.kubernetes.io/part-of: rfpt
spec:
  selector:
    app.kubernetes.io/name: rfpt-performance
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
