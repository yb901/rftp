apiVersion: v1
kind: Service
metadata:
  name: rfpt-mng
  namespace: prod
  labels:
    app.kubernetes.io/name: rfpt-mng
    app.kubernetes.io/part-of: rfpt
spec:
  selector:
    app.kubernetes.io/name: rfpt-mng
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
