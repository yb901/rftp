apiVersion: v1
kind: Service
metadata:
  name: rf-performance
  namespace: prod
  labels:
    app.kubernetes.io/name: rf-performance
    app.kubernetes.io/part-of: rf
spec:
  selector:
    app.kubernetes.io/name: rf-performance
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
