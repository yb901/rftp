apiVersion: apps/v1
kind: Deployment
metadata:
  name: rfpt-mng
  namespace: prod
  labels:
    app.kubernetes.io/name: rfpt-mng
    app.kubernetes.io/part-of: rfpt
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: rfpt-mng
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rfpt-mng
        app.kubernetes.io/part-of: rfpt
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: rfpt-mng
          image: RFPT_MNG_IMAGE_PLACEHOLDER
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 18091
          env:
            - name: TZ
              value: Asia/Shanghai
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: LOG_PATH
              value: /app/logs
            - name: APP_VERSION
              value: APP_VERSION_PLACEHOLDER
            - name: RFPT_DB_URL
              valueFrom:
                secretKeyRef:
                  name: rfpt-backend-db-secret
                  key: url
            - name: RFPT_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: rfpt-backend-db-secret
                  key: username
            - name: RFPT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rfpt-backend-db-secret
                  key: password
            - name: DUBBO_REGISTRY_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: rfpt-backend-config
                  key: dubbo-registry-address
            - name: JAVA_OPS
              value: "-Xms512m -Xmx1g -XX:MetaspaceSize=128m -XX:MaxMetaspaceSize=256m -XX:+UseZGC -XX:+ZGenerational -XX:+UseStringDeduplication -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/app/logs -XX:+ExitOnOutOfMemoryError -Dfile.encoding=UTF-8 -Duser.timezone=Asia/Shanghai -Duser.home=/app"
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 45
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 90
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "50m"
              memory: "1Gi"
            limits:
              cpu: "1"
              memory: "2Gi"
