apiVersion: v1
kind: Service
metadata:
  name: rfpt-h5-node
  namespace: prod
  labels:
    app.kubernetes.io/name: rfpt-h5-node
    app.kubernetes.io/part-of: rfpt
spec:
  selector:
    app.kubernetes.io/name: rfpt-h5-node
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
